
* We are only interested in searching movies.

* Storing API key on the client for simplicity (at least its hidden from github in an .env variable).

* Did not use redux as we are keeping it simple (KISS).

* Usability has a token effort, it requires more work.

* Multi-language support is not required.

